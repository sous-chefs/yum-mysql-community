---
driver:
  name: vagrant

transport:
  max_wait_until_ready: 600
  connection_retries: 10
  connection_retry_sleep: 10

provisioner:
  name: chef_infra
  product_name: chef
  product_version: <%= ENV['CHEF_VERSION'] || 'latest' %>
  channel: stable
  chef_license: accept
  enforce_idempotency: <%= ENV['ENFORCE_IDEMPOTENCY'] || true %>
  multiple_converge: <%= ENV['MULTIPLE_CONVERGE'] || 2 %>
  deprecations_as_errors: true

verifier:
  name: inspec
  sudo: true

platforms:
  - name: almalinux-8
  - name: almalinux-9
  - name: almalinux-10
  - name: centos-stream-9
  - name: centos-stream-10
  - name: fedora-42
  - name: oraclelinux-8
  - name: oraclelinux-9
  - name: rockylinux-8
  - name: rockylinux-9
  - name: rockylinux-10

x-run_lists:
  default: &default_run_list
    - recipe[test::default]
  mysql80: &mysql80_run_list
    - recipe[test::mysql80]
  connectors: &connectors_run_list
    - recipe[test::connectors]
  innovation: &innovation_run_list
    - recipe[test::innovation]

x-verifiers:
  default: &default_verifier
    inspec_tests:
      - path: test/integration/default
  mysql80: &mysql80_verifier
    inspec_tests:
      - path: test/integration/mysql80
  connectors: &connectors_verifier
    inspec_tests:
      - path: test/integration/connectors
  innovation: &innovation_verifier
    inspec_tests:
      - path: test/integration/innovation

suites:
  - name: default
    run_list: *default_run_list
    verifier: *default_verifier

  - name: mysql80
    provisioner:
      named_run_list: mysql80
    run_list: *mysql80_run_list
    verifier: *mysql80_verifier
    excludes:
      - almalinux-10
      - centos-stream-10
      - fedora-42
      - rockylinux-10

  - name: connectors
    provisioner:
      named_run_list: connectors
    run_list: *connectors_run_list
    verifier: *connectors_verifier

  - name: innovation
    provisioner:
      named_run_list: innovation
    run_list: *innovation_run_list
    verifier: *innovation_verifier
